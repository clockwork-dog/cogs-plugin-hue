{{#ifCond templateType '===' "react"}}import react from "@vitejs/plugin-react";
{{/ifCond}}import { defineConfig } from "vite";
import cogsSdkPlugin from "vite-plugin-cogs-sdk";

export default defineConfig({
  base: "./",
  build: {
    sourcemap: true,
  },
  plugins: [{{#ifCond templateType '===' "react"}}react(), {{/ifCond}}cogsSdkPlugin()],
});
