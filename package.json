{
  "name": "cogs-sdk-example",
  "private": true,
  "scripts": {
    "start": "turbo run dev",
    "build": "turbo run build-prod",
    "check-repo": "sherif && turbo boundaries",
    "check-types": "turbo run types",
    "check-lint": "turbo run lint",
    "test": "sherif && turbo boundaries && turbo run types lint",
    "new": "turbo gen cogs-sdk && yarn install"
  },
  "devDependencies": {
    "@turbo/gen": "^2.5.4",
    "es-toolkit": "^1.39.6",
    "eslint": "^9.30.1",
    "jiti": "^2.4.2",
    "prettier": "^3.6.2",
    "prettier-plugin-organize-imports": "^4.1.0",
    "sherif": "^1.6.1",
    "turbo": "^2.5.4",
    "typescript": "~5.8.3"
  },
  "overrides": {
    "@types/node": "^22.16.0"
  },
  "packageManager": "yarn@1.22.22",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
